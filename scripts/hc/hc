#!/usr/bin/env bash
# Thin wrapper so "hc" is the main HC entrypoint.
# Delegates to the hc-master.sh next to this file.

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

exec "${SCRIPT_DIR}/hc-master.sh" "$@"
