Change: Standardize workspace and automation MCP server names to avoid collisions with user/global defaults
Date: 2026-04-11
Branch: chore/workspace-governance-monitor

Context
- VS Code reported an MCP name collision for `io.github.netdata/mcp-server` between workspace `.vscode/mcp.json` and the user's global `C:\Users\jcasn\AppData\Roaming\Code\User\mcp.json`.
- Goal: eliminate the conflict and define a naming convention that prevents similar collisions going forward.

Plan
- Rename workspace MCP server keys to a `homelab-*` convention.
- Rename root automation MCP keys to a `HOMELAB_*` convention for parity.
- Update docs and governor guidance to codify the naming rule.
- Re-run workspace health and tooling currency verification.

Results
- The conflicting workspace key `io.github.netdata/mcp-server` was replaced with `homelab-netdata-cloud`.
- All workspace MCP keys now use the `homelab-*` naming convention.
- All root automation MCP keys now use the `HOMELAB_*` naming convention.
- `python scripts/session/workspace_health.py --strict --report artifacts/automation/workspace-health-report.json` reported:
  - `missing markdown links: 0`
  - `json errors: 0`
  - `secret findings: 0`
- `python scripts/session/tooling_currency.py --report artifacts/automation/tooling-currency-report.json` reported:
  - `latest tag findings: 0`
  - `mcp servers: 7`
  - `workspace_mcp_naming_issue_count: 0`
  - `root_mcp_naming_issue_count: 0`
