# Documentation Color Palette Update

- Updated `mkdocs.yml` to replace the hardcoded static `slate` color theme with a media query-based responsive scheme.
- Added a light/dark mode toggle.
- Ensures the documentation site respects user system preferences, improving overall accessibility.
- Verified site build successfully using `mkdocs build`.
