Change: Workspace hardening, evidence-rule reinforcement, MCP/devcontainer hygiene, and autonomy workplan.
Date: 2026-04-10
Workspace: z:\\

Verified evidence before edits:
- Devcontainer root cause: Docker Desktop WSL root filesystem is full.
  - `/dev/sde 135.1M size, 132.1M used, 0 available, 100% use`
  - `/root/.vscode-remote-containers` consumes `61.5M`
- Documentation audit: `TOTAL_DOCS_MISSING=0` for `docs/` and `WIKI/` local markdown links.

Planned changes:
- Add workspace instruction file reinforcing artifact/evidence requirements.
- Create a staged implementation workplan for autonomous workspace evolution.
- Harden `.vscode/settings.json`, `.vscode/extensions.json`, and MCP configuration.
- Improve `.devcontainer` configuration and reduce avoidable image churn.
- Verify repo hygiene and targeted devcontainer remediation.

Executed changes:
- Added `.github/instructions/evidence-first.instructions.md`.
- Added stage-specific agents for docs hygiene, devcontainer triage, and infra pipeline planning.
- Updated `mcp.json` to use environment-based secret references for automation.
- Updated `.vscode/mcp.json` to remove tracked tokens and use prompted secret inputs.
- Updated `.vscode/settings.json` and `.vscode/extensions.json` for workspace health and tooling.
- Updated `.devcontainer/devcontainer.json` and `.devcontainer/Dockerfile` for a more capable and repeatable sandbox.
- Created `plan/process-autonomous-workspace-1.md`.

Verification after changes:
- Docker Desktop WSL space restored after targeted cache cleanup:
  - `/dev/sde 135.1M size, 70.5M used, 54.0M available, 57% use`
  - `/root/.vscode-remote-containers` reduced to `2.0K`
- Devcontainer image smoke build succeeded:
  - `docker build -f .devcontainer/Dockerfile .devcontainer -t homelab-config-devcontainer-smoke`
  - Build completed through export and image naming with no errors.
- VS Code file validation reports `No errors found` for all updated JSON, Markdown, and agent files.
