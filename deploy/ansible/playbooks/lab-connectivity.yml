---
- name: Validate homelab connectivity from the controller
  hosts: proxmox:lxc
  gather_facts: false
  tasks:
    - name: Include non-destructive connectivity role
      ansible.builtin.include_role:
        name: lab_connectivity
