---
# 2026-07-12 CR-0414: deploy target for scripts/hc/*; content is pushed here
# by hc-pipeline-deploy.yml, never edited in place on the host.
hc_deploy_root: /srv/homelab-share/homelab-config
proxmox_read_only: true
proxmox_discovery_commands:
  - pveversion
  - pct list
  - qm list
  - ip -br link
  - bridge link show
  - bridge vlan show
proxmox_required_evidence:
  - artifacts/hc/*-alpha-live-discovery.txt
  - artifacts/hc/*-lab-reachability.txt
  - artifacts/hc/*-proxmox-api-probe.txt
